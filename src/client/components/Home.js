import React from 'react';

const Home = () => {
  return  (
    <div>
      <div className="">I'm the VERY BEST home component</div>
      <button onClick={() => console.log('Hi there!')}>Click me!</button>
    </div>
  );
};

export default Home;